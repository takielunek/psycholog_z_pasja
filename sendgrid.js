// import sgMail from "@sendgrid/mail";

// sgMail.setApiKey(process.env.SENDGRID_API_KEY);

// const options = {
//   to: "haponax110@ofionk.com",
//   from: email,
//   subject: "Nowa konsultacja",
//   html: `
//       <p>${name}</p>
//       <p>${email}</p>
//       <p>${phone}</p>
//       <p>${message}</p>
//       `,
// };

// sgMail.send(options).then(
//   () => {},
//   (error) => {
//     console.error(error);

//     if (error.response) {
//       console.error(error.response.body);
//     }
//   }
// );
